server:
  port: 8080

openai:
  api-key: ${OPENAI_API_KEY}
  base-url: https://openrouter.ai/api
  model: deepseek/deepseek-chat
  timeout: 60

spring:
  application:
    name: adcopy-backend

logging:
  level:
    com.adcopy: DEBUG